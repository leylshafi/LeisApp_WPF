<UserControl x:Class="Source.Views.UserControls.ExploreViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:usercontrols="clr-namespace:Source.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Canvas Grid.Row="0">
            <Border BorderThickness="5" Height="40" Width="350" BorderBrush="AliceBlue" Canvas.Left="75" Background="LightGray" CornerRadius="18" >
                <usercontrols:TextBoxPlaceHolder PlaceHolderText="Search Twitter" Margin="5" Height="35" Width="350" />
            </Border>
        </Canvas>

    </Grid>
</UserControl>
